# Student API using MVC Structure

This project implements a simple Student API using Node.js and Express.

## Folder Structure
- controllers → contains API logic
- routes → contains endpoint routes
- server.js → starts the server

## API Endpoints

GET /students  
Returns the list of students.

POST /students  
Adds a new student.

DELETE /students/:id  
Deletes a student by ID.
